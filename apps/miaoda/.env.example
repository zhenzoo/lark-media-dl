# Server-only. Use the same app OpenAPI key as the Worker.
# Obtain your own key scoped to the three routes in docs/openapi.json.
# Set this in Miaoda online environment variables; never in client/VITE_* variables.
MEDIA_WORKER_API_KEY=
# The Miaoda CLI supplies database and platform identity into local .env.local.
# Do not copy the maintainer's .env.local or .spark/meta.json.
